//ci c'est le contect de notre app

//vient de la biblio react et qui  permet d'initialiser notre contexte avec des valeurs par défaut.
import { createContext } from "react";

export const UidContext = createContext();
